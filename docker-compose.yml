version: '3.8'

services:
  eulerq-platform:
    build:
      context: .
      dockerfile: Dockerfile
    image: eulerq-eta-platform:latest
    container_name: eulerq_container
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
